{
  "name": "cmh-react",
  "version": "1.0.0",
  "private": true,
  "description": "Chris Michael Harris — WordPress to React migration",
  "scripts": {
    "dev": "next dev",
    "prebuild": "node scripts/sync-covers.mjs && node scripts/gen-site-images.mjs",
    "build": "next build",
    "start": "next start -p ${PORT:-3000}",
    "lint": "next lint",
    "sync:covers": "node scripts/sync-covers.mjs",
    "migrate:images": "node scripts/migrate-images.mjs",
    "postbuild": "node scripts/validate-schema.mjs",
    "validate:schema": "node scripts/validate-schema.mjs"
  },
  "dependencies": {
    "@next/third-parties": "^16.3.0",
    "gray-matter": "^4.0.3",
    "next": "16.2.12",
    "next-mdx-remote": "^5.0.0",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
